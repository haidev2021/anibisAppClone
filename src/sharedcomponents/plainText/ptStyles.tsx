
import {
    StyleSheet,
} from 'react-native';
import { darkPlainText } from '../../utils/xbaseInterface.d';

export const PTStyles = StyleSheet.create({
    plain_text: {
        fontSize: 15,
        color: darkPlainText,
    },
});