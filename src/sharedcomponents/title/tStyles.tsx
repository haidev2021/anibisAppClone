
import {
    StyleSheet,
} from 'react-native';
import { darkPlainText } from '../../utils/xbaseInterface.d';

export const TStyles = StyleSheet.create({
    title: {
        fontSize: 20,
        color: darkPlainText,
    },
});