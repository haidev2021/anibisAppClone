import { actionTypes } from '../actions/actionTypes';

let reportState = {
}
export const defaultReducer = (state = reportState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
}